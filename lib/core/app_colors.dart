import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary — Teal ──────────────────────────────
  static const Color primary = Color(0xFF01696F);
  static const Color primaryDark = Color(0xFF0C4E54);
  static const Color primaryDeep = Color(0xFF0F3638);
  static const Color primaryLight = Color(0xFFCEDCD8);
  static const Color primarySurface = Color(0xFFE8F4F4);

  // ── Accent — Saffron ────────────────────────────
  static const Color accent = Color(0xFFE07B39);
  static const Color accentDark = Color(0xFFC05A1A);
  static const Color accentLight = Color(0xFFFFF0E6);

  // ── Semantic ─────────────────────────────────────
  static const Color income = Color(0xFF2E7D32); // Green
  static const Color incomeLight = Color(0xFFE8F5E9);
  static const Color expense = Color(0xFFC62828); // Red
  static const Color expenseLight = Color(0xFFFFEBEE);
  static const Color warning = Color(0xFFFF8F00);
  static const Color warningLight = Color(0xFFFFFDE7);

  // ── Light Theme Surfaces ─────────────────────────
  static const Color bgLight = Color(0xFFF7F6F2);
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surface2Light = Color(0xFFF9F8F5);
  static const Color dividerLight = Color(0xFFE0DDD8);
  static const Color borderLight = Color(0xFFD4D1CA);

  // ── Dark Theme Surfaces ──────────────────────────
  static const Color bgDark = Color(0xFF171614);
  static const Color surfaceDark = Color(0xFF1C1B19);
  static const Color surface2Dark = Color(0xFF201F1D);
  static const Color dividerDark = Color(0xFF262523);
  static const Color borderDark = Color(0xFF393836);

  // ── Text ─────────────────────────────────────────
  static const Color textDark = Color(0xFF28251D);
  static const Color textMutedDark = Color(0xFF7A7974);
  static const Color textFaintDark = Color(0xFFBAB9B4);
  static const Color textLight = Color(0xFFCDCCCA);
  static const Color textMutedLight = Color(0xFF797876);
}
